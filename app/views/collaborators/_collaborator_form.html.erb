<% User.all.each do |u| %>
  <% if !wiki.users.include?(u) %>
  <div class="row">
    <%= form_for Collaborator.new do |f| %>
      <%= f.hidden_field :user_id, value: u.id %>
      <%= f.hidden_field :wiki_id, value: wiki.id %>
      <%= u.email %> <%= f.submit 'Make Collaborator', class: "btn btn-success" %>
    <% end %>
  </div>
  <% else %>
    <div class="row">
    <%= collaborator_id %> <%= link_to "Remove Collaborator", collaborator_id, method: :delete, class: 'btn btn-danger', data: {confirm: "Are you sure you want to remove this Collborator?"} %>
  <% end %>
<% end %>
